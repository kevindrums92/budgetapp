# Supabase Configuration
# Get these values from: https://supabase.com/dashboard → Your Project → Settings → API

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key-here

# Instructions:
# 1. Copy this file to .env.local (for production) or .env.dev (for development)
# 2. Replace the values with your actual Supabase credentials
# 3. Never commit .env.local or .env.dev to git

# =====================================================
# Edge Functions Secrets (set via Supabase Dashboard)
# =====================================================
# These secrets must be configured in Supabase Dashboard:
# Edge Functions → Select function → Secrets
#
# FIREBASE_SERVICE_ACCOUNT - JSON string of Firebase service account
#   1. Go to Firebase Console → Project Settings → Service Accounts
#   2. Click "Generate new private key"
#   3. Copy the entire JSON content
#   4. Set as FIREBASE_SERVICE_ACCOUNT secret in Supabase
#
# Note: SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY are automatically
# available in Edge Functions, no need to set them manually.
